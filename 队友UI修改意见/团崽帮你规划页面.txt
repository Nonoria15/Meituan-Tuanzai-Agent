Create a complete static mobile homepage (390px width, centered) for a local life app with the following components stacked vertically:
1. Feature icon grid (8 entries with local image files and fallback inline SVGs) – no breathing dot
2. AI Smart Trip Planner card
3. **"Weekend Helper" card** (based on the provided image “周末帮我排”, with updated subheading)
4. Bottom tab bar (static, tabs: 首页, 视频, 小团, 购物车, 我的)

All styles follow a unified Global Design System using CSS custom properties defined in `:root`. Use exactly the local image file paths specified; if an image fails to load, fallback to the provided inline SVG data URI.

## Global Design System (define in :root)
```css
:root {
  --font-h1: 22px;
  --font-body: 14px;
  --font-caption: 12px;
  --font-small: 11px;
  --font-tab-label: 10px;
  --weight-bold: 700;
  --weight-medium: 500;
  --weight-regular: 400;
  --line-height-title: 1.3;
  --line-height-body: 1.4;

  --color-text-primary: #1F1F1F;
  --color-text-secondary: #6B6B6B;
  --color-text-tertiary: #4A4A4A;
  --color-text-label: #333333;
  --color-bg-page: #F8F8F8;
  --color-bg-card: #FFFFFF;
  --color-bg-tag: #F2F2F2;
  --color-bg-capsule: #F5F5F5;
  --color-bg-input: #F2F2F2;
  --color-bg-friend: #F9F9F9;
  --color-success-bg: #E8F5E9;
  --color-success-text: #2E7D32;
  --color-primary: #ffd100;
  --color-tab-bg: #ffffff;
  --color-tab-border: rgba(0, 0, 0, 0.05);
  --color-tab-label-default: #777777;
  --color-tab-label-active: #1f1f1f;
  --color-tab-active-bg: linear-gradient(180deg, #fff8d8, #ffffff);
  --color-tab-underline: #ffd100;

  --space-4: 4px;
  --space-8: 8px;
  --space-12: 12px;
  --space-16: 16px;
  --space-20: 20px;
  --space-24: 24px;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --radius-full: 30px;
  --radius-grid-card: 18px;
  --radius-icon-img: 10px;
  --radius-tab-item: 12px;

  --shadow-card: 0 2px 8px rgba(0,0,0,0.04);
  --shadow-grid-card: 0 8px 22px rgba(18,18,18,0.06);
  --shadow-tab-bar: 0 -8px 24px rgba(18, 18, 18, 0.045);

  --icon-size: 28px;
  --grid-columns: repeat(4, minmax(0, 1fr));
  --grid-gap: 12px;
  --tab-icon-size: 22px;
  --tab-item-min-height: 46px;
  --tab-underline-width: 18px;
  --tab-underline-height: 3px;
}
All components MUST use these variables. No hardcoded sizes/colors. Spacing must be multiples of 4px.

Page Layout
Outer wrapper: .phone class, width 390px, background var(--color-bg-page), min-height 700px, display flex, flex-direction column, border-radius 32px, overflow hidden, box-shadow large, position relative.

Content area: .content class, flex 1, overflow-y auto, padding var(--space-16) var(--space-16) 0.

Bottom tab bar stays at the bottom naturally (margin-top auto in flex column).

Component 1: Feature Icon Grid (8 entries) – No breathing dot
(Keep exactly the same as previously defined, without any dot pulse.)

Component 2: AI Smart Trip Planner Card
(Keep exactly the same as previously defined, with badge, title, subtitle, capsules, input, friend row.)

Component 3: Weekend Helper Card (based on the provided image “周末帮我排”)
Card: .weekend-helper-card – same base styles as ai-planner-card: background var(--color-bg-card), border-radius var(--radius-lg), padding var(--space-16), box-shadow var(--shadow-card), margin-top var(--space-12), margin-bottom var(--space-12).

Contents (top to bottom, following the image layout with updated subheading):

Main title

Text: "周末帮我排"

Font-size: var(--font-h1) (22px), weight: var(--weight-bold), color: var(--color-text-primary), line-height: var(--line-height-title), margin-bottom: var(--space-4).

Subtitle / tagline

Text: "团崽 AI 正在接入美团小团本地生活工具链"

Font-size: var(--font-caption) (12px), weight: var(--weight-medium), color: var(--color-text-secondary), margin-bottom: var(--space-16).

Subheading – replaced with "团崽帮你规划"

Text: "团崽帮你规划"

Font-size: var(--font-body) (14px), weight: var(--weight-bold), color: var(--color-text-primary), margin-bottom: var(--space-8).

(This replaces the original “告诉我你的周末目标”. It aligns with the title style of other cards, ensuring visual consistency.)

Description text

Text: "告诉团崽时间、预算、同行人和偏好，我来帮你把周末安排好。"

Font-size: var(--font-body), weight: var(--weight-regular), color: var(--color-text-secondary), line-height: var(--line-height-body), margin-bottom: var(--space-16).

Example input / text box

<input type="text" value="今晚和朋友聚会，想先玩再吃饭，气氛热闹一些，最好同一个商圈解决。" class="plain-input" />

Styling: width 100%, height auto (min-height 44px), background var(--color-bg-input), border none, border-radius var(--radius-full), padding var(--space-12) var(--space-16), font-size var(--font-body), color var(--color-text-primary), box-sizing border-box.

Margin-bottom: var(--space-16).

Button "开始规划"

<button class="btn-primary">开始规划</button>

Full width, height 44px, background var(--color-primary), border none, border-radius var(--radius-lg), font var(--font-body) var(--weight-medium), color #1F1F1F, margin-bottom: var(--space-20), cursor pointer, active opacity 0.8.

Quick scenarios section

Title: "快捷场景" – font-size var(--font-caption), color var(--color-text-secondary), margin-bottom var(--space-8).

Capsule container: .capsule-group flex wrap, gap var(--space-8).

Capsules (text only): "情侣约会半日游", "亲子周末计划", "朋友聚会吃喝玩乐", "雨天室内方案", "低预算不踩雷".

Margin-bottom: var(--space-16).

Recommended questions section

Title: "推荐问题" – font-size var(--font-caption), color var(--color-text-secondary), margin-bottom var(--space-8).

Question list: .question-list flex column, gap var(--space-12).

Each question item: font-size var(--font-body), color var(--color-text-primary), line-height var(--line-height-body). Prefix with "· ".

Two questions:

"附近 3 小时轻松约会怎么安排？"
"餐厅满座时能自动换同价位选择吗？"
Component 4: Bottom Tab Bar (Static)
(Keep exactly the same as previously defined.)

Global Rules
All spacing uses variables from the :root. The spacing values (e.g., --space-8, --space-16, --space-20) ensure consistent vertical rhythm between elements. All titles and subheadings are left-aligned, matching the card's padding.

The page uses flex column layout, with the bottom nav pushed to the bottom using margin-top: auto.

Provide one complete, self-contained HTML file with embedded CSS. No external dependencies.

Use exactly the local image paths specified for icons; include fallback SVGs as described.