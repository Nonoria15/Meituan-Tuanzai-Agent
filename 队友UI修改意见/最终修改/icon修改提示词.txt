markdown
## 任务：替换美团风格首页“团崽 AI · 周末帮我排”卡片中的两个图标

请在现有代码中找到以下两个图标，并将其替换为本地图片：

### 1. 输入框内的魔棒图标（出行关键词 icon）
- **位置**：位于“下午带娃出去，4-6小时...”输入框内部或紧邻右侧（通常是一个小魔棒图标，用于唤起语音或关键词输入）。
- **替换要求**：
  - 图片路径：`images/icon_keyword.png`
  - 不显示文字标签
  - 保持原有图标尺寸（约 20×20px）和位置（输入框右侧内部或外部）
- **注意事项**：如果原图标是字体图标或SVG，请将其改为 `<img src="images/icon_keyword.png">`，并移除原有图标代码。

### 2. 同行人信息框内的图标（多人出行 icon）
- **位置**：位于“同行人：未选择”这一行的左侧或右侧（通常是一个人头像或多人图标）。
- **替换要求**：
  - 图片路径：`images/icon_multi.png`
  - 不显示文字标签
  - 保持原有图标尺寸（约 20×20px）和对齐方式（与文字水平居中）
- **注意事项**：如果原图标是字体图标或SVG，请同样替换为 `<img>` 标签。

### 输出要求
提供修改后的相关 HTML/CSS 代码片段（只涉及这两个图标的替换部分），确保图片正确引用且页面布局不变。同时添加注释说明用户需要将 `icon_keyword.png` 和 `icon_multi.png` 放入 `images/` 文件夹。

请保留所有其他内容（输入框文字、同行人文字、按钮等）不变。