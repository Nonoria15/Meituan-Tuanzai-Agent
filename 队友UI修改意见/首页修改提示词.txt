markdown
Create a complete static mobile homepage (width 390px, centered) for a local life app. The page contains three main components stacked vertically:
1. Feature icon grid (8 entries using local image files)
2. **Meituan Xiao Tuan AI Trip Planner Card** (updated content based on the provided image)
3. Bottom tab bar (static, no interaction, tabs: 首页、视频、小团、购物车、我的)

All styles follow a unified Global Design System defined with CSS custom properties in `:root`.

## Global Design System (define in :root)
```css
:root {
  /* Typography */
  --font-h1: 22px;
  --font-body: 14px;
  --font-caption: 12px;
  --font-small: 11px;
  --font-tab-label: 10px;
  --weight-bold: 700;
  --weight-medium: 500;
  --weight-regular: 400;
  --line-height-title: 1.3;
  --line-height-body: 1.4;

  /* Colors */
  --color-text-primary: #1F1F1F;
  --color-text-secondary: #6B6B6B;
  --color-text-tertiary: #4A4A4A;
  --color-text-label: #333333;
  --color-bg-page: #F8F8F8;
  --color-bg-card: #FFFFFF;
  --color-bg-tag: #F2F2F2;
  --color-bg-capsule: #F5F5F5;
  --color-bg-input: #F2F2F2;
  --color-bg-friend: #F9F9F9;
  --color-success-bg: #E8F5E9;
  --color-success-text: #2E7D32;
  --color-primary: #ffd100;               /* Meituan yellow for buttons */

  /* Tab bar */
  --color-tab-bg: #ffffff;
  --color-tab-border: rgba(0, 0, 0, 0.05);
  --color-tab-label-default: #777777;
  --color-tab-label-active: #1f1f1f;
  --color-tab-active-bg: linear-gradient(180deg, #fff8d8, #ffffff);
  --color-tab-underline: #ffd100;

  /* Spacing (4px grid) */
  --space-4: 4px;
  --space-8: 8px;
  --space-12: 12px;
  --space-16: 16px;
  --space-20: 20px;
  --space-24: 24px;

  /* Border radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --radius-grid-card: 18px;
  --radius-icon-img: 10px;
  --radius-full: 30px;
  --radius-tab-item: 12px;

  /* Shadows */
  --shadow-card: 0 2px 8px rgba(0,0,0,0.04);
  --shadow-grid-card: 0 8px 22px rgba(18,18,18,0.06);
  --shadow-tab-bar: 0 -8px 24px rgba(18, 18, 18, 0.045);

  /* Feature icon grid */
  --icon-size: 28px;
  --grid-columns: repeat(4, minmax(0, 1fr));
  --grid-gap: 12px;

  /* Tab bar dimensions */
  --tab-icon-size: 22px;
  --tab-item-min-height: 46px;
  --tab-underline-width: 18px;
  --tab-underline-height: 3px;
}
All components MUST use these variables. No hardcoded pixel values, colors, or fonts outside the variable definitions. Spacing must be multiples of 4px (border-width can be 0.5px).

Page Layout
Outer wrapper: max-width: 390px; margin: 0 auto; background: var(--color-bg-page); min-height: 100vh; display: flex; flex-direction: column;

Content area: flex: 1; overflow-y: auto; padding: var(--space-16) var(--space-16) 0;

Bottom tab bar: placed after the content area, pushed to bottom using margin-top: auto.

Component 1: Feature Icon Grid (8 entries)
A white card with a 4-column grid of icons. All icon images are local files placed in an images/ folder. The code must reference them exactly as specified.

Card (class feature-grid-card)

Margin: -12px 12px 0 (top -12px, left/right 12px)

Padding: var(--space-12)

Background: var(--color-bg-card)

Border-radius: var(--radius-grid-card)

Box-shadow: var(--shadow-grid-card)

Grid

Display: grid; grid-template-columns: var(--grid-columns); gap: var(--grid-gap);

Each item: display flex; flex-direction column; align-items center; justify-content center; position relative;

Image file mapping (name and path required)
Each entry must use the following local image file name inside images/:

Name	Image file (relative path)
外卖	images/icon_waimai.png
到店美食	images/icon_food.png
休闲娱乐	images/icon_play.png
电影演出	images/icon_movie.png
酒店预订	images/icon_hotel.png
旅游门票	images/icon_ticket.png
超市闪购	images/icon_mart.png
生活服务	images/icon_service.png
Use <img> with width and height var(--icon-size), object-fit cover, border-radius var(--radius-icon-img), and the exact src as shown above.

Text label: font-size var(--font-small), weight var(--weight-medium), color var(--color-text-label), margin-top var(--space-4).

Note: No breathing dot effect is required on any icon.

Component 2: Meituan Xiao Tuan AI Trip Planner Card (updated based on image)
Card (class xiaotuan-planner-card)

Background customization note:
This card uses a default white background (var(--color-bg-card)) but you can replace it with a color image. To do so, add a custom class or inline style: background-image: url('images/card_bg.png'); background-size: cover; background-position: center;. The code includes a comment to guide developers.

Default background: var(--color-bg-card)

Border-radius: var(--radius-lg)

Padding: var(--space-16)

Box-shadow: var(--shadow-card)

Margin-top: var(--space-12)

Contents (top to bottom):

Badge line (small tag)

Text: "基于美团小团"

Display: inline-block; background: var(--color-bg-tag); border-radius: var(--radius-xl); font-size: var(--font-caption); font-weight: var(--weight-medium); color: var(--color-text-tertiary); padding: var(--space-4) var(--space-12); margin-bottom: var(--space-8)

Main title

Text: "团崽 AI · 周末帮我排"

Font-size: var(--font-h1); font-weight: var(--weight-bold); color: var(--color-text-primary); line-height: var(--line-height-title); margin-bottom: var(--space-8)

Subtitle

Text: "基于美团小团升级，帮你规划多人吃喝玩乐路线。"

Font-size: var(--font-body); font-weight: var(--weight-regular); color: var(--color-text-secondary); line-height: var(--line-height-body); margin-bottom: var(--space-16)

Feature capsules (4 text-only pills, flex wrap)

Labels: "多人偏好协调", "实时排队预约", "路线预算规划", "异常兜底"

Each: background var(--color-bg-capsule); border-radius var(--radius-full); padding var(--space-8) var(--space-16); font-size var(--font-caption); font-weight var(--weight-medium); color var(--color-text-primary); no border.

Container: display flex; flex-wrap wrap; gap var(--space-8);

Margin-bottom: var(--space-16)

Text input (no icons)

<input type="text" placeholder="下午带娃出去，4-6小时..." />

Width 100%; height 44px; background var(--color-bg-input); border none; border-radius var(--radius-full); padding var(--space-12) var(--space-16); font-size var(--font-body); color var(--color-text-primary); outline none; box-sizing border-box.

Margin-bottom: var(--space-12)

Friend / companion row

Container: background var(--color-bg-friend); border-radius var(--radius-md); padding var(--space-12); display flex; align-items center; justify-content space-between; font-size var(--font-body); color var(--color-text-primary).

Left text: "👥 同行人：小雨、妈妈"

Right badge: "已授权", background var(--color-success-bg); color var(--color-success-text); border-radius var(--radius-sm); padding 2px 8px; font-size var(--font-small); font-weight var(--weight-medium).

Margin-bottom: var(--space-16)

Action button

Text: "生成方案"

<button class="btn-generate">生成方案</button>

Width 100%; height 44px; background var(--color-primary); border none; border-radius var(--radius-lg); font-size var(--font-body); font-weight var(--weight-medium); color #1F1F1F; cursor pointer.

Add :active opacity 0.8 for touch feedback.

CSS for the button (add to global styles):

css
.btn-generate {
  width: 100%;
  height: 44px;
  background: var(--color-primary);
  border: none;
  border-radius: var(--radius-lg);
  font-size: var(--font-body);
  font-weight: var(--weight-medium);
  color: #1F1F1F;
  cursor: pointer;
  transition: opacity 0.2s;
}
.btn-generate:active {
  opacity: 0.8;
}
Additional comment for background image replacement – insert an HTML comment inside the card:

html
<!-- To replace the card background with a custom color/image, add: style="background-image: url('images/card_bg.png'); background-size: cover;" -->
Component 3: Bottom Tab Bar (Static, 5 tabs: 首页、视频、小团、购物车、我的)
Container (class bottom-nav)

Background: var(--color-tab-bg);

Border-top: 0.5px solid var(--color-tab-border);

Box-shadow: var(--shadow-tab-bar);

Display: grid; grid-template-columns: repeat(5, 1fr);

Padding: var(--space-8) var(--space-4) var(--space-8);

margin-top: auto;

Each tab item (class tab-item)

Display: flex; flex-direction column; align-items center; justify-content center; min-height: var(--tab-item-min-height); border-radius: var(--radius-tab-item); position: relative; background: transparent;

Active state (class active): background: var(--color-tab-active-bg);

Active underline:

css
.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 50%;
  transform: translateX(-50%);
  width: var(--tab-underline-width);
  height: var(--tab-underline-height);
  background-color: var(--color-tab-underline);
  border-radius: 2px;
}
Icons – use local image files placed in images/ folder
Tab icon file mapping (relative path required):

Tab Name	Image file
首页	images/icon_home.png
视频	images/icon_video.png
小团	images/icon_tuanzi.png
购物车	images/icon_cart.png
我的	images/icon_profile.png
Use <img> with class tab-icon, width and height var(--tab-icon-size), object-fit contain, display block, margin-bottom var(--space-4).

Default opacity: 0.6 on .tab-icon, and .tab-item.active .tab-icon opacity: 1.

Label

<span> class tab-label, font-size: var(--font-tab-label), weight: var(--weight-medium), color: var(--color-tab-label-default).

Active tab label color: var(--color-tab-label-active).

Tab order and active state:

首页 (active)

视频

小团

购物车

我的

Global Rules
All spacing uses variables from the :root.

The page uses flex column layout, with the bottom nav pushed to the bottom using margin-top: auto.

Provide one complete, self-contained HTML file with embedded CSS. No external dependencies.

Use exactly the local image paths specified; do not use any placeholder URLs from external services.

Add comments in the HTML noting that users must place the corresponding icon images in an images/ subfolder.

Include a comment for the card background image replacement as described above.